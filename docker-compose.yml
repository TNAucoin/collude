version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: provision/docker/api/Dockerfile
      target: development
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
